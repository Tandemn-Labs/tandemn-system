# Server & API
fastapi
uvicorn[standard]
python-dotenv
requests

# Placement (AWS allocation, LLM advisors)
pandas
openai
tabulate
termcolor
torch
transformers

# Cloud / job launch
skypilot[aws]

# Storage (S3 presign/multipart used by server)
boto3

# Config / data
PyYAML

# Solver dependencies (swap this line to use a different solver)
-r LLM_placement_solver/requirements.txt