### General

Shouldn't push output files or other large data files to Git.::wq

### server.py

launch config + fallback config -> Seems like compatibility added to support original LLM +CPMI approach. Should probably default to list of configs in order of how they should be tried. If LLM+CPMI approach spits out one, just use a list of one element

